"use client";

import type { StepProps } from "./InitialSetupWizard";

// Placeholder — implemented in a later phase.
export function StepSiteScan(_props: StepProps) {
  return (
    <div className="flex min-h-0 flex-1 items-center justify-center text-zinc-500">
      Site &amp; Scan Type — coming next
    </div>
  );
}
