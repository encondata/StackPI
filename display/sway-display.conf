# StackPI status-display sway config — single-window kiosk.
default_border          none
default_floating_border none
hide_edge_borders       both
focus_follows_mouse     no
seat * hide_cursor      2000

# Boot splash: the StackPI logo on the brand background, shown the instant sway
# starts and left up until the chromium kiosk window paints over it (~20-30s of
# software rendering on a Pi 3). Installed by bootstrap to a stable path.
output * background /opt/stackpi-display/logo.png center #0d0d12

# The display browser window (chromium --class=stackpi-display).
for_window [app_id="stackpi-display"] fullscreen enable

# Recovery: hold Ctrl+Shift+Alt+Q on an attached keyboard to exit.
bindsym Ctrl+Shift+Mod1+q exit

exec /usr/local/bin/display-kiosk-launch.sh
